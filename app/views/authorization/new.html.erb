<%= form_for :authorization, :url => oauth_authorize_path(params.slice(:redirect_uri, :client_id, :client_secret)) do |form| %>
<h3>Authorize MY_APP to access your account</h3>
<%= form.submit "Yes", :value => 'Yes'  %>
<%= form.submit "No", :value => 'No'  %>
<% end %>